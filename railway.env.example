# Railway Environment Variables Template
# Copy these to your Railway dashboard for each service

# ========================================
# BACKEND SERVICE ENVIRONMENT VARIABLES
# ========================================

# Node Environment (required)
NODE_ENV=production

# Port (Railway auto-provides this, do not set manually)
# PORT=${{PORT}}

# Frontend URL (set to your frontend service URL)
FRONTEND_URL=${{FRONTEND_SERVICE_URL}}
# Or use your custom domain:
# FRONTEND_URL=https://vivid-valid.yourdomain.com

# Logging
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Optional: SMTP Configuration (if using SMTP testing)
# SMTP_TIMEOUT=5000
# SMTP_FROM_DOMAIN=validator.yourdomain.com

# Optional: Caching
# ENABLE_CACHE=true
# CACHE_TTL=300

# ========================================
# FRONTEND SERVICE ENVIRONMENT VARIABLES
# ========================================

# Node Environment (required)
NODE_ENV=production

# Backend API URL (set to your backend service URL)
VITE_API_BASE_URL=${{BACKEND_SERVICE_URL}}/api
# Or use your custom domain:
# VITE_API_BASE_URL=https://api.yourdomain.com/api

# ========================================
# RAILWAY TEMPLATE VARIABLES
# ========================================

# Railway provides these automatically:
# ${{PORT}} - The port your service should listen on
# ${{RAILWAY_ENVIRONMENT}} - The environment name (production, staging, etc.)
# ${{RAILWAY_PUBLIC_DOMAIN}} - Your service's public domain
# ${{RAILWAY_PRIVATE_DOMAIN}} - Your service's private domain
# ${{RAILWAY_PROJECT_ID}} - Your project ID
# ${{RAILWAY_SERVICE_ID}} - Your service ID
# ${{RAILWAY_SERVICE_NAME}} - Your service name
# ${{RAILWAY_DEPLOYMENT_ID}} - Current deployment ID
# ${{RAILWAY_GIT_COMMIT_SHA}} - Git commit SHA
# ${{RAILWAY_GIT_BRANCH}} - Git branch name

# Service References (use these to connect services):
# ${{BACKEND_SERVICE_URL}} - Reference to backend service URL
# ${{FRONTEND_SERVICE_URL}} - Reference to frontend service URL

# ========================================
# SETUP INSTRUCTIONS
# ========================================

# 1. Go to Railway Dashboard: https://railway.app/dashboard
# 2. Select your project
# 3. For Backend Service:
#    - Click on backend service
#    - Go to "Variables" tab
#    - Add the backend variables listed above
#
# 4. For Frontend Service:
#    - Click on frontend service
#    - Go to "Variables" tab
#    - Add the frontend variables listed above
#
# 5. After setting variables, redeploy both services
#
# 6. Generate public domains:
#    - Click on backend service -> Settings -> Generate Domain
#    - Click on frontend service -> Settings -> Generate Domain
#
# 7. Update CORS settings if using custom domains

# ========================================
# CUSTOM DOMAIN SETUP (Optional)
# ========================================

# If using custom domains:
# 1. Add CNAME record: api.yourdomain.com -> your-backend.up.railway.app
# 2. Add CNAME record: vivid-valid.yourdomain.com -> your-frontend.up.railway.app
# 3. Update environment variables to use custom domains
# 4. Configure custom domains in Railway dashboard

# ========================================
# SECURITY NOTES
# ========================================

# - Never commit actual environment values to git
# - Use Railway's environment variable feature for all secrets
# - Rotate sensitive values regularly
# - Use different values for production and staging
# - Enable rate limiting in production
# - Set appropriate CORS origins in backend